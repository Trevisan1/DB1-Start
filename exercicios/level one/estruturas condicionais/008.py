print('Digite o valor do salário atual e o valor do reajuste e eu direi o valor reajustado')

salario_atual = int(input('Digite o salário atual: '))

reajuste = int (input('Digite o valor do reajuste: '))

total = salario_atual + reajuste

print(f'O valor reajustado é de: {total}')